{
  "buildCommand": "[ \"$VERCEL_ENV\" = \"production\" ] && npx supabase@latest link --project-ref \"$SUPABASE_PROJECT_REF\" && npx supabase@latest db push --password \"$SUPABASE_DB_PASSWORD\" || true; pnpm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/apps/web/src/app/api/$1"
    }
  ]
}

