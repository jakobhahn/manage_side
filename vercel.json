{
  "buildCommand": "[ \"$VERCEL_ENV\" = \"production\" ] && (command -v supabase || npm i -g supabase) && supabase link --project-ref \"$SUPABASE_PROJECT_REF\" && supabase db push --password \"$SUPABASE_DB_PASSWORD\" || true; pnpm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "apps/web/src/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/apps/web/src/app/api/$1"
    }
  ]
}

